1. Optimize (string literals, for instance).
2. More test strings
3. XML Documentation
4. Convert to and from DateTime with a conversion exception if a date is uncertain, unknown, etc.
5. Helper methods for ExtendedDateTimePossibilityCollection such as last possible date, first possible date, and time span of possible dates.
6. Constructor Overloads to make initialization more convienient.