1. Optimize (e.g. string literals, redundant code, etc.).
2. Add more comprehensive tests.
3. Consider TimeZone offset in ExtendedDateTime calculations.
4. Make Local UTC Offset the default UTC Offset when parsing extended datetimes without a specified offset.
5. Replace IExtremityProvider with implementations of IEnumerable<ExtendedDateTime> on each object.
6. Upgrade tests from string output to UserControls for each entry with green or red highlighting indicating pass/fail. 